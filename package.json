{
  "name": "obsidian-meeting-tasks",
  "version": "3.2.2",
  "description": "Dynamic Gmail task extraction with configurable label processors for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.js",
    "dev:watch": "node esbuild.config.js --watch",
    "build": "tsc --noEmit && node esbuild.config.js production",
    "build:release": "NODE_ENV=production node build.js",
    "build:dev": "node build.js",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist main.js",
    "release": "npm run clean && npm run build:release",
    "deploy": "./deploy.sh"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "meeting",
    "tasks",
    "ai"
  ],
  "author": "Jim Allen <jimallen@gmail.com>",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "obsidian": "^1.8.7",
    "tslib": "^2.8.1",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@types/moment": "^2.11.29",
    "@types/moment-timezone": "^0.5.13",
    "moment": "^2.30.1",
    "moment-timezone": "^0.6.0"
  }
}
